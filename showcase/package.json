{
  "name": "showcase",
  "private": true,
  "version": "0.0.0",
  "description": "GitHub Pages demo: this repo's own docs, rendered as a static site and searched by @ktjn/searchable-client (docs/archive/roadmaps/github-pages-showcase.md).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build:docs": "tsx build-docs.ts",
    "build:widget": "tsc -p tsconfig.widget.json",
    "build:search": "tsx build-search.ts",
    "build:gallery": "tsx build-gallery.ts",
    "build:gallery-synonyms": "tsx build-gallery-synonyms.ts",
    "build:gallery-i18n": "tsx build-gallery-i18n.ts",
    "build:gallery-index": "tsx build-gallery-index.ts",
    "build": "pnpm build:docs && pnpm build:widget && pnpm build:search && pnpm build:gallery && pnpm build:gallery-synonyms && pnpm build:gallery-i18n && pnpm build:gallery-index",
    "validate": "tsx validate-dist.ts",
    "check": "pnpm build && pnpm validate && pnpm test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "marked": "^18.0.6"
  },
  "devDependencies": {
    "@ktjn/searchable-indexer": "workspace:*",
    "@types/node": "^26.1.1",
    "tsx": "^4.19.2",
    "typescript": "^7.0.2"
  }
}
